# ktisis-docs
documentation hub for ktisis' 0.4 release onwards: https://docs.ktisis.tools/

## building
serve to local webserver for testing:
```
python -m mkdocs serve
```

build for static site release:
```
python -m mkdocs build
```
